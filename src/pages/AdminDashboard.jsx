// src/pages/AdminDashboard.jsx (CORRECTED - Empty page)
import React from 'react';
import './AdminDashboard.css';

const AdminDashboard = () => {
  return (
    <div className="admin-dashboard">
      {/* Empty page - all controls are in the header */}
      {/* Add your admin features here later */}
    </div>
  );
};

export default AdminDashboard;